{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar COBOL",
            "type": "shell",
            "command": "cobc -x -o ${fileDirname}/${fileBasenameNoExtension} ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
