       IDENTIFICATION DIVISION.
       PROGRAM-ID. DOC_GENERATOR.
       AUTHOR. ANDRES CAMILO LAGUNA.
       DATE-WRITTEN. 14-02-2025.
       
       ENVIRONMENT DIVISION.
       INPUT-OUTPUT SECTION.
       FILE-CONTROL.
           SELECT DOCFILE ASSIGN TO "documentacion.txt"
               ORGANIZATION IS LINE SEQUENTIAL.
       
       DATA DIVISION.
       FILE SECTION.
       FD  DOCFILE.
       01  DOC-RECORD       PIC X(250).
       
       WORKING-STORAGE SECTION.
       01  WS-DOC-LINE      PIC X(250) VALUE SPACES.
       
       PROCEDURE DIVISION.
       MAIN-PROGRAM.
           OPEN OUTPUT DOCFILE.
           
           STRING "------------------------------------------------------------" DELIMITED BY SIZE
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "DOCUMENTACIÓN DEL COMANDO DISPLAY EN COBOL" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "Autor: ANDRES CAMILO LAGUNA" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "Fecha: 14-02-2025" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "------------------------------------------------------------" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "Tabla de Contenidos:" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "1. Introducción" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "2. Propósito" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "3. Sintaxis" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "4. Ejemplos" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "5. Buenas Prácticas" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "6. Conclusiones" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "------------------------------------------------------------" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "1. Introducción:" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "El comando DISPLAY en COBOL es fundamental para" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "mostrar información en la salida estándar. Se utiliza" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "para interactuar y depurar programas, mostrando mensajes" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "y valores de variables de forma inmediata." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "2. Propósito:" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "- Visualizar mensajes y valores." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "- Facilitar la depuración." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "- Generar salidas formateadas." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "3. Sintaxis:" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "DISPLAY literal-or-identifier" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "   [UPON device] [WITH NO ADVANCING]." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "4. Ejemplos:" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "Ejemplo 1: DISPLAY ""Hola, mundo!"". " DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "Ejemplo 2: " DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "   MOVE ""Bienvenido a COBOL"" TO WS-MENSAJE," DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "   DISPLAY WS-MENSAJE." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "Ejemplo 3: " DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "   DISPLAY ""Nombre: "" WITH NO ADVANCING," DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "   ACCEPT WS-NOMBRE, DISPLAY ""Bienvenido, "" WS-NOMBRE." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "5. Buenas Prácticas:" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "- Utilizar mensajes claros y descriptivos." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "- Emplear WITH NO ADVANCING solo cuando sea necesario." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "- Verificar el dispositivo indicado con UPON." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "6. Conclusiones:" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "DISPLAY es esencial para la interacción y depuración en COBOL." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "Su correcto uso mejora la claridad y el mantenimiento de" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "los programas COBOL." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "------------------------------------------------------------" DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "Fin del Documento." DELIMITED BY SIZE
                 INTO WS-DOC-LINE.
           WRITE DOC-RECORD FROM WS-DOC-LINE.
           
           STRING "-------------------------------------
